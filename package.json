{
  "name": "promernstack2",
  "version": "1.0.0",
  "description": "Pro Mern Stack 2 Book work along",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -w server server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel src/App.jsx --out-dir public",
    "watch": "babel src/App.jsx --out-dir public --watch --verbose"
  },
  "author": "Bryan Hain",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.4",
    "@babel/preset-react": "^7.16.0"
  }
}
